<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard | FixIt</title>
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
        crossorigin="anonymous"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            min-height: 100vh;
            padding-bottom: 3rem;
        }

        nav {
            background: rgba(44, 62, 80, 0.95) !important;
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.2);
            padding: 1rem 0;
        }

        .nav-link {
            font-weight: 600;
            color: rgba(255, 255, 255, 0.9) !important;
            padding: 0.5rem 1rem !important;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            background: rgba(255, 255, 255, 0.1);
            color: white !important;
        }

        .btn-outline-light {
            border: 2px solid rgba(255, 255, 255, 0.5);
            color: white;
            font-weight: 600;
            padding: 0.5rem 1.5rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .btn-outline-light:hover {
            background: white;
            color: #2c3e50;
            transform: translateY(-2px);
        }

        .complaints-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .page-header {
            text-align: center;
            margin-bottom: 3rem;
            padding-top: 2rem;
        }

        .page-header h1 {
            font-size: 2.5rem;
            font-weight: 800;
            color: white;
            margin-bottom: 0.5rem;
        }

        .page-header p {
            color: rgba(255, 255, 255, 0.8);
            font-size: 1.1rem;
        }

        .complaint-card {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }

        .complaint-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.3);
        }

        .complaint-date {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            display: inline-block;
            font-size: 0.85rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
        }

        .card-title-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #e2e8f0;
        }

        .card-title-section h5 {
            font-size: 1.5rem;
            font-weight: 700;
            color: #2d3748;
            margin: 0;
        }

        .btn-view-image {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-weight: 600;
            font-size: 0.85rem;
            transition: all 0.3s ease;
        }

        .btn-view-image:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(245, 87, 108, 0.4);
        }

        .info-section {
            margin-bottom: 1.5rem;
        }

        .info-label {
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }

        .info-value {
            color: #718096;
            font-size: 0.9rem;
        }

        .info-value a {
            color: #2c3e50;
            text-decoration: none;
            margin-left: 0.5rem;
            transition: all 0.3s ease;
        }

        .info-value a:hover {
            color: #34495e;
        }

        .form-section {
            background: #f7fafc;
            padding: 1.5rem;
            border-radius: 12px;
            margin-bottom: 1.5rem;
        }

        .form-section-title {
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 1rem;
            font-size: 1rem;
        }

        .radio-group {
            display: flex;
            gap: 1.5rem;
            flex-wrap: wrap;
        }

        .form-check {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .form-check-input {
            width: 1.25rem;
            height: 1.25rem;
            border: 2px solid #cbd5e0;
            cursor: pointer;
        }

        .form-check-input:checked {
            background-color: #2c3e50;
            border-color: #2c3e50;
        }

        .form-check-label {
            font-weight: 600;
            color: #2d3748;
            cursor: pointer;
        }

        select {
            padding: 0.5rem 1rem;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            font-size: 0.9rem;
            font-weight: 600;
            color: #2d3748;
            background: white;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        select:focus {
            outline: none;
            border-color: #2c3e50;
            box-shadow: 0 0 0 4px rgba(44, 62, 80, 0.1);
        }

        .btn-submit {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            border: none;
            padding: 0.75rem 2rem;
            border-radius: 8px;
            font-weight: 700;
            transition: all 0.3s ease;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(44, 62, 80, 0.4);
        }

        .action-buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 2px solid #e2e8f0;
        }

        .btn-user-profile {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 0.5rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn-user-profile:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .btn-delete {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .btn-delete:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(245, 87, 108, 0.4);
        }

        @media (max-width: 768px) {
            .complaint-card {
                padding: 1.5rem;
            }
            
            .page-header h1 {
                font-size: 2rem;
            }
            
            .radio-group {
                flex-direction: column;
                gap: 0.75rem;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid px-4">
            <a class="navbar-brand" href="#">
                <span style="color: white; font-weight: 800; font-size: 1.5rem;">
                    FixIt <i class="bi bi-shield-check"></i>
                </span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" href="/admin/user-list">
                            <i class="bi bi-people me-1"></i>User List
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/admin/appoint-officer">
                            <i class="bi bi-person-plus me-1"></i>Appoint Officer
                        </a>
                    </li>
                </ul>
                <button class="btn btn-outline-light logout_btn ms-3" type="button">
                    <i class="bi bi-box-arrow-right me-1"></i>Logout
                </button>
            </div>
        </div>
    </nav>

    <div class="complaints-container">
        <div class="page-header">
            <h1><i class="bi bi-clipboard-data"></i> Admin Dashboard</h1>
            <p>Manage and review all complaints</p>
        </div>

        <% complaints.forEach(e=> { %>
            <div class="complaint-card">
                <div class="complaint-date">
                    <i class="bi bi-calendar3 me-1"></i><%= e.date %>
                </div>

                <form action="/admin-complaint/<%=e._id %>" method="post">
                    <div class="card-title-section">
                        <h5><%= e.title %></h5>
                        <a href="<%= e.imageURL %>" target="_blank">
                            <button type="button" class="btn-view-image">
                                <i class="bi bi-image me-1"></i>View Image
                            </button>
                        </a>
                    </div>

                    <div class="info-section">
                        <div class="info-label"><i class="bi bi-geo-alt me-1"></i>Address</div>
                        <div class="info-value">
                            <%= e.address %>
                            <a href="<%= e.gMapURL %>" target="_blank">
                                <i class="bi bi-map"></i>
                            </a>
                        </div>
                    </div>

                    <div class="form-section">
                        <div class="form-section-title"><i class="bi bi-check-circle me-1"></i>Approval Status</div>
                        <div class="radio-group">
                            <% if (e.approvalStatus==="Pending" ) { %>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="pending_<%=e._id%>" value="Pending" checked>
                                    <label class="form-check-label" for="pending_<%=e._id%>">Pending</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="yes_<%=e._id%>" value="Yes">
                                    <label class="form-check-label" for="yes_<%=e._id%>">Approve</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="no_<%=e._id%>" value="No">
                                    <label class="form-check-label" for="no_<%=e._id%>">Reject</label>
                                </div>
                            <% } else if (e.approvalStatus==="Yes" ) { %>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="pending_<%=e._id%>" value="Pending">
                                    <label class="form-check-label" for="pending_<%=e._id%>">Pending</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="yes_<%=e._id%>" value="Yes" checked>
                                    <label class="form-check-label" for="yes_<%=e._id%>">Approve</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="no_<%=e._id%>" value="No">
                                    <label class="form-check-label" for="no_<%=e._id%>">Reject</label>
                                </div>
                            <% } else { %>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="pending_<%=e._id%>" value="Pending">
                                    <label class="form-check-label" for="pending_<%=e._id%>">Pending</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="yes_<%=e._id%>" value="Yes">
                                    <label class="form-check-label" for="yes_<%=e._id%>">Approve</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="approval" id="no_<%=e._id%>" value="No" checked>
                                    <label class="form-check-label" for="no_<%=e._id%>">Reject</label>
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div class="info-section">
                        <div class="info-label"><i class="bi bi-tools me-1"></i>Current Work Status</div>
                        <div class="info-value"><%= e.workStatus %></div>
                    </div>

                    <div class="form-section">
                        <div class="form-section-title"><i class="bi bi-gear me-1"></i>Update Work Status</div>
                        <select name="workStatus" class="w-100">
                            <option>~</option>
                            <option value="-">Unset</option>
                            <option value="Started">Work Started</option>
                            <option value="Finished">Work Finished</option>
                        </select>
                    </div>

                    <div class="info-section">
                        <div class="info-label"><i class="bi bi-person-badge me-1"></i>Assigned Officer</div>
                        <div class="info-value"><%= e.officerAppointed %></div>
                    </div>

                    <div class="form-section">
                        <div class="form-section-title"><i class="bi bi-person-plus me-1"></i>Assign Officer</div>
                        <select name="officer" class="w-100">
                            <option>~</option>
                            <option value="-">Unselect</option>
                            <% officers.forEach(officer => { %>
                                <option value="<%= officer.officerName %>"><%= officer.officerName %></option>
                            <% }) %>
                        </select>
                    </div>

                    <div class="d-flex justify-content-end mt-3">
                        <button type="submit" class="btn-submit">
                            <i class="bi bi-check-circle me-1"></i>Commit Changes
                        </button>
                    </div>
                </form>

                <div class="action-buttons">
                    <form action="/admin-user-dashboard-delete" method="post">
                        <input type="hidden" name="userId" value="<%= e.userID %>">
                        <button type="submit" class="btn-user-profile" name="button" value="dashboard">
                            <i class="bi bi-person-circle me-1"></i>View User Profile
                        </button>
                    </form>
                    <form action="/delete-complaint/<%=e.id%>" method="post" name="myform">
                        <button type="submit" class="btn-delete" name="dashboard" value="adminDashboard">
                            <i class="bi bi-trash me-1"></i>Delete
                        </button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>

    <script>
        let logout_btn = document.querySelector(".logout_btn");
        logout_btn.addEventListener("click", () => {
            window.location.replace("/sub-pages/login.html");
        });
    </script>
</body>
</html>

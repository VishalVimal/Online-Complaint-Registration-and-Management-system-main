<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Officer Dashboard | FixIt</title>
    <link rel="shortcut icon" href="/images/logo.png" type="image/x-icon" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      body {
        font-family: 'Inter', sans-serif;
        background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        min-height: 100vh;
        padding: 2rem;
      }

      .logout-btn {
        position: fixed;
        top: 2rem;
        right: 2rem;
        z-index: 1000;
      }

      .btn-dark {
        background: rgba(44, 62, 80, 0.9);
        backdrop-filter: blur(10px);
        color: white;
        border: 2px solid rgba(255, 255, 255, 0.2);
        padding: 0.75rem 1.5rem;
        border-radius: 12px;
        font-weight: 600;
        transition: all 0.3s ease;
      }

      .btn-dark:hover {
        background: rgba(44, 62, 80, 1);
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
      }

      .complaints-container {
        max-width: 1000px;
        margin: 0 auto;
        padding-top: 4rem;
      }

      .page-header {
        text-align: center;
        margin-bottom: 3rem;
        color: white;
      }

      .page-header h1 {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 0.5rem;
        text-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
      }

      .page-header p {
        font-size: 1.1rem;
        opacity: 0.9;
      }

      .complaint-card {
        background: white;
        border-radius: 20px;
        margin-bottom: 2rem;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        overflow: hidden;
        transition: all 0.3s ease;
      }

      .complaint-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 50px rgba(0, 0, 0, 0.3);
      }

      .complaint-date {
        background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        color: white;
        padding: 0.5rem 1rem;
        font-size: 0.85rem;
        font-weight: 600;
        display: inline-block;
        margin: 1rem;
        border-radius: 20px;
      }

      .card-header {
        background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
        padding: 1.5rem;
        border-bottom: 2px solid #e2e8f0;
      }

      .card-header-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: #2d3748;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .card-header-title a {
        color: #11998e;
        text-decoration: none;
        font-size: 1.25rem;
        transition: all 0.3s ease;
      }

      .card-header-title a:hover {
        color: #38ef7d;
        transform: scale(1.1);
      }

      .list-group-item {
        padding: 1.25rem 1.5rem;
        border: none;
        border-bottom: 1px solid #e2e8f0;
      }

      .list-group-item:last-child {
        border-bottom: none;
      }

      .info-label {
        font-weight: 700;
        color: #2d3748;
        margin-bottom: 0.5rem;
        font-size: 0.95rem;
      }

      .info-value {
        color: #718096;
        font-size: 0.9rem;
      }

      .info-value a {
        color: #11998e;
        text-decoration: none;
        margin-left: 0.5rem;
        transition: all 0.3s ease;
      }

      .info-value a:hover {
        color: #38ef7d;
      }

      .form-section {
        background: #f7fafc;
        padding: 1.5rem;
        border-radius: 12px;
        margin-top: 1rem;
      }

      .form-section-title {
        font-weight: 700;
        color: #2d3748;
        margin-bottom: 1rem;
        font-size: 1rem;
      }

      select {
        width: 100%;
        padding: 0.75rem 1rem;
        border: 2px solid #e2e8f0;
        border-radius: 8px;
        font-size: 0.9rem;
        font-weight: 600;
        color: #2d3748;
        background: white;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      select:focus {
        outline: none;
        border-color: #11998e;
        box-shadow: 0 0 0 4px rgba(17, 153, 142, 0.1);
      }

      .file-upload {
        margin-top: 1rem;
      }

      .file-upload-label {
        background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        color: white;
        padding: 0.75rem 1.5rem;
        border-radius: 8px;
        font-weight: 600;
        display: inline-block;
        margin-bottom: 0.75rem;
      }

      .form-control-file {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid #e2e8f0;
        border-radius: 8px;
        font-size: 0.9rem;
        transition: all 0.3s ease;
      }

      .form-control-file:focus {
        outline: none;
        border-color: #11998e;
        box-shadow: 0 0 0 4px rgba(17, 153, 142, 0.1);
      }

      .btn-submit {
        background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        color: white;
        border: none;
        padding: 0.75rem 2rem;
        border-radius: 8px;
        font-weight: 700;
        transition: all 0.3s ease;
        margin-top: 1rem;
      }

      .btn-submit:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(17, 153, 142, 0.4);
      }

      @media (max-width: 768px) {
        .complaints-container {
          padding-top: 6rem;
        }
        
        .page-header h1 {
          font-size: 2rem;
        }
        
        .logout-btn {
          top: 1rem;
          right: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="logout-btn">
      <button type="button" class="btn btn-dark logout_btn">
        <i class="bi bi-box-arrow-right me-1"></i>Logout
      </button>
    </div>

    <div class="complaints-container">
      <div class="page-header">
        <h1><i class="bi bi-briefcase-fill"></i> Officer Dashboard</h1>
        <p>Update work status for assigned complaints</p>
      </div>

      <div class="complaints">
        <% complaints.forEach(e => { %>
          <div class="complaint-card">
            <div class="complaint-date">
              <i class="bi bi-calendar3 me-1"></i><%= e.date %>
            </div>

            <form action="/officer-update-complaint" method="post" enctype="multipart/form-data">
              <div class="card-header">
                <div class="card-header-title">
                  <span><%= e.title %></span>
                  <a href="<%= e.imageURL %>" target="_blank" title="View Complaint Image">
                    <i class="bi bi-image"></i>
                  </a>
                </div>
              </div>

              <ul class="list-group list-group-flush">
                <li class="list-group-item">
                  <div class="info-label"><i class="bi bi-geo-alt-fill me-1"></i>Address</div>
                  <div class="info-value">
                    <%= e.address %>
                    <a href="<%= e.gMapURL %>" target="_blank">
                      <i class="bi bi-map"></i>
                    </a>
                  </div>
                </li>

                <li class="list-group-item">
                  <div class="info-label"><i class="bi bi-tools me-1"></i>Current Work Status</div>
                  <div class="info-value"><%= e.workStatus %></div>
                </li>

                <li class="list-group-item">
                  <div class="form-section">
                    <div class="form-section-title"><i class="bi bi-gear me-1"></i>Update Work Status</div>
                    <select name="workStatus" required>
                      <option>~</option>
                      <option value="-">Unset</option>
                      <option value="Started">Work Started</option>
                      <option value="Finished">Work Finished</option>
                    </select>

                    <div class="file-upload">
                      <label class="file-upload-label">
                        <i class="bi bi-upload me-1"></i>Upload Work Status Image
                      </label>
                      <input type="file" class="form-control-file" id="photo_<%=e.id%>" name="workStatusImage" accept="image/*" required />
                    </div>

                    <div class="d-flex justify-content-end">
                      <button type="submit" class="btn-submit">
                        <i class="bi bi-check-circle me-1"></i>Commit Changes
                      </button>
                    </div>

                    <input type="hidden" name="complaintId" value="<%= e.id %>" />
                    <input type="hidden" name="officerName" value="<%= e.officerAppointed %>" />
                  </div>
                </li>
              </ul>
            </form>
          </div>
        <% }) %>
      </div>
    </div>

    <script>
      let logout_btn = document.querySelector(".logout_btn");
      logout_btn.addEventListener("click", () => {
        window.location.replace("/sub-pages/login.html");
      });
    </script>
  </body>
</html>
